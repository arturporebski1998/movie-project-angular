import { ClassMethod } from '@angular/compiler';

export class Movie {
    
    constructor(
      id: number,
      title: string,
      genre: string,
      director: string,
      date: number,
      avgRate: number,
      country: string,
      description: string,
      ){}
  }